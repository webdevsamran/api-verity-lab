openapi: 3.1.0
info:
  title: Verity 2020-12 Fixture API
  version: 1.0.0
  description: >-
    Exercises the JSON Schema 2020-12 keywords the model used to parse away and
    drop. Every one of them states a rule the document makes, so a diff that
    cannot see it reports a tightened contract as unchanged.
paths:
  /shipments:
    post:
      operationId: createShipment
      summary: Book a shipment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reference]
              properties:
                reference:
                  type: string
                card:
                  type: string
                cvv:
                  type: string
                mode:
                  type: string
                  enum: [air, sea, road]
                weightKg:
                  type: number
              # Sending a card number obliges you to send the code with it.
              dependentRequired:
                card: [cvv]
              # Vendor extensions on the request are free-form strings.
              patternProperties:
                "^x-":
                  type: string
              # A road shipment has to declare a weight.
              if:
                type: object
                required: [mode]
                properties:
                  mode:
                    const: road
              then:
                type: object
                required: [weightKg]
      responses:
        "201":
          description: Booked
          content:
            application/json:
              schema:
                type: object
                required: [id, route]
                properties:
                  id:
                    type: string
                  # A route is a tuple: origin, destination, then optional stops.
                  route:
                    type: array
                    prefixItems:
                      - type: string
                        description: Origin port code.
                      - type: string
                        description: Destination port code.
                    items:
                      type: string
                    minItems: 2
                  legs:
                    type: array
                    contains:
                      type: object
                      required: [carrier]
                      properties:
                        carrier:
                          type: string
                    minContains: 1
                  labels:
                    type: object
                    propertyNames:
                      type: string
                      pattern: "^[a-z][a-z0-9-]*$"
                    additionalProperties:
                      type: string
